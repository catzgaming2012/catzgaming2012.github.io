{
  "type": "module",
  "dependencies": {
    "bigdecimal.js": "^1.3.1"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "copy": "shx cp -r docs/* dist",
    "build": "npm run clean && mkdir -p dist/host && npm run copy"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}
