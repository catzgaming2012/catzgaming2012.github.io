{
  "dependencies": {
    "bigdecimal.js": "1.3.1"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  },
  "scripts": {
    "clean": "shx rm -rf docs/",
    "copy-all": "shx cp -r * docs/",
    "build": "npm run clean && npm run copy-all"
  }
}
