{
  "name": "a-stupid-website",
  "version": "1.0.0",
  "description": "It's right what it says on the tin:\n Stupid",
  "dependencies": {
    "express": "^5.2.1"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "copy": "mkdir -p dist && cp -r docs/. dist",
    "build": "npm run clean && npm run copy",
    "build-vercel": "cd .. && npm run build",
    "dev": "nodemon ../.dev/app.js"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "shx": "^0.4.0"
  }
}
